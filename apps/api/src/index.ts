// API server — placeholder. Full implementation in Section 5.
// This file validates that the scaffold compiles.

const PORT = process.env.PORT ?? 3001;

// eslint-disable-next-line no-console
console.log(`API scaffold ready. Will start on port ${PORT} after Section 5.`);
